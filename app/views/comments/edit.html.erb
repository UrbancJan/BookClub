<div class="edit">
<h1>Editing Comment</h1>

<%= form_for([@book, @comment]) do |f|%>
  <div class="field">
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<%= link_to 'Back', (book_path (@book)) %>
</div>