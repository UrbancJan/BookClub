
<p>Posted by <b><%=comment.user.email %></b> <%= time_ago_in_words(comment.created_at) %> ago</p>

<p><%=comment.content %></p>

<% if user_signed_in? %>
<% if comment.user_id == current_user.id %>
<%= link_to "Edit", edit_book_comment_path(comment.book, comment) %>
<%= link_to "Delete", book_comment_path(comment.book, comment), method: :delete, data: {confirm: "Are you sure?"}%>

<%end%>
<%end%>